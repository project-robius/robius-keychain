use crate::{Identifier, KeychainItemBuilder, Result};

pub fn store(item: KeychainItemBuilder) -> Result<()> {
    todo!();
}

pub fn load(identifier: Identifier) -> Result<Option<String>> {
    todo!();
}
